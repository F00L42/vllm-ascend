# 导入来源与完整性

- 会话：生成迁移文档。
- 会话 ID：`6aace1e2-da84-83ed-a2ad-e7fe157a2e6f`。
- 来源：[ChatGPT 会话](https://chatgpt.com/c/6aace1e2-da84-83ed-a2ad-e7fe157a2e6f)。
- 获取日期：2026-09-18。
- 范围：会话列出的 6 份文档正文，全部已读取并保存。

## 获取方式

会话读取接口只返回附件占位符；网页可以打开全部单份文档预览。ZIP `mega_migration_910b_2026-09-18.zip` 下载失败，提示无法获取上传状态；单份原始下载也未成功。因此本目录保存的是**文档预览正文导出并转换的 Markdown**，不声称与原始附件字节一致，也没有伪造 ZIP。

每份文档的正文 DOM HTML 保存在 `_preview/`，只截取文档内容，不含会话侧边栏、其他聊天或临时下载凭据。通过 `markdownify==1.2.0` 转为 Markdown；转换保留标题、列表、表格、代码和链接。预览中未提供代码语言标签的代码块统一标注为 `text`；原有 Mermaid 内容仍作为源码保存。

已使用 Markdown 渲染回读逐份比较：去除空白后的正文一致；全部链接目标及顺序一致（比较时解码 URL 百分号编码）。这能检查文本和链接完整性，不能证明原附件的空白、Markdown 写法、代码语言标记或文件元数据完全相同。

[IMPORT_MANIFEST.json](IMPORT_MANIFEST.json) 记录 6 份 Markdown 与 HTML 的 SHA-256 及检查结果。后续发现原附件时，另存原件并补充校验，不覆盖此导入证据。

## 使用口径

正文保留原会话内容，不随开发进度改写。历史短 SHA、API、建议目录和拟新增开关仍需对当前源码核对。本轮新方案位于 `../../mega-gdn/`，实际结果应进入独立轮次报告。
